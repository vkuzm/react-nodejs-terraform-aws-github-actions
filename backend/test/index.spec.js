  const expect = require('chai').expect;

describe('set of unit tests', function () {
    it('unit test 1', function () {
        expect(2 + 2).to.equal(4);
    });

    it('unit test 2', function () {
        expect(2 * 3).to.equal(6);
    });

    it('unit test 3', function () {
        expect(2 / 2).to.equal(1);
    });
});
